{
  "name": "@geld/action",
  "version": "0.1.0",
  "description": "GitHub Action that maintains one Geld review-summary comment on a pull request.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "esbuild src/main.ts --bundle --platform=node --format=cjs --outfile=dist/index.js --legal-comments=none --banner:js='// Generated by pnpm --filter @geld/action build — do not edit.'",
    "check": "tsc --noEmit && vitest run && pnpm build"
  },
  "dependencies": {
    "@geld/review": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3",
    "vitest": "^5.0.0"
  }
}
